SELECT DISTINCT ?uri ?name ?thumbnail ?indegree ?info ?date WHERE{    {    SELECT DISTINCT    ?name ?uri ?thumbnail    SAMPLE(?info) AS ?info    SAMPLE(?date) AS ?date    COUNT(distinct ?isKindOf) AS ?count    COUNT(distinct ?type) AS ?indegree WHERE {	?uri dbo:deathDate ?date		FILTER (( datatype(?date) = xsd:date ) ||			( datatype(?date) = xsd:dateTime )).	  	FILTER (regex(str(?date), datum) ).        ?uri rdf:type ?isKindOf	    FILTER( (regex(str(?isKindOf),"ontology") )).   	?uri rdfs:label ?name		FILTER(langMatches(lang(?name), "EN")).	?uri dbp:shortDescription ?info.	?type ?property ?uri.   	OPTIONAL {?uri dbo:thumbnail ?thumbnail }.        }	}FILTER(?count = 4)} ORDER BY DESC(?indegree)